<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <title>Oferty pracy</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
      font-size: 28px;
    }

    a.add-offer {
      display: inline-block;
      margin-bottom: 20px;
      background-color: #28a745; /* zielony przycisk Dodaj */
      color: white;
      padding: 10px 16px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 2px 6px rgba(40,167,69,0.4);
      transition: background-color 0.3s ease;
    }

    a.add-offer:hover {
      background-color: #1e7e34;
    }

    hr {
      width: 100%;
      max-width: 700px;
      border: none;
      border-top: 1px solid #ccc;
      margin-bottom: 20px;
    }

    .offer {
      background-color: white;
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 15px 20px;
      margin-bottom: 15px;
      width: 100%;
      max-width: 700px;
      box-sizing: border-box;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    .offer h2 {
      margin: 0 0 10px 0;
      color: #222;
      font-size: 22px;
    }

    .offer p {
      color: #555;
      margin: 6px 0;
      line-height: 1.4;
    }

    .actions {
      margin-top: 15px;
    }

    .actions a,
    .actions button {
      display: inline-block;
      font-size: 14px;
      font-weight: 600;
      padding: 8px 14px;
      border-radius: 6px;
      cursor: pointer;
      text-decoration: none;
      border: none;
      transition: background-color 0.3s ease, color 0.3s ease;
      margin-right: 10px;
      background-color: #007bff;
      color: white;
      box-shadow: 0 2px 5px rgba(0,123,255,0.4);
      user-select: none;
    }

    .actions a:hover,
    .actions button:hover {
      color: white;
      box-shadow: 0 3px 12px rgba(0,86,179,0.5);
    }
    .actions a:hover {
            background-color: #27ae60;
        }
        .actions button:hover {
            background-color: #c0392b;
        }
    .back-link {
    display: inline-block;
    margin-top: 30px;
    color: #007bff;
    font-weight: 600;
    text-decoration: none;
    transition: color 0.3s ease;
  }
  .back-link:hover {
    color: #0056b3;
  }

    @media (max-width: 720px) {
      body, .offer {
        padding: 10px;
        width: 95%;
      }
    }
  </style>
</head>
<body>
  <h1>Oferty pracy</h1>
  <a class="add-offer" href="/admin/career/add">Dodaj nową ofertę</a>
  <hr />

  <% if (offers.length > 0) { %>
    <% offers.forEach(offer => { %>
      <div class="offer">
        <h2><%= offer.title %></h2>
        <p><strong>Wynagrodzenie:</strong> <%= offer.salary %></p>
        <p><%= offer.description.substring(0, 100) %>...</p>
        <div class="actions">
          <a href="/admin/career/edit/<%= offer.id %>">Edytuj</a>
          <form action="/admin/career/delete/<%= offer.id %>" method="POST" style="display:inline;" onsubmit="return confirm('Usunąć ofertę?');">
            <button type="submit">Usuń</button>
          </form>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <p>Brak ofert pracy.</p>
  <% } %>

  <a href="/admin" class="back-link">Powrót do panelu</a>
</body>
</html>
