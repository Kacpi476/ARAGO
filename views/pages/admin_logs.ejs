<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <title>Logi prób logowania</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f7fb;
      margin: 20px;
      color: #333;
    }
    h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #007bff;
    }
    table {
      border-collapse: collapse;
      width: 100%;
      max-width: 900px;
      margin: 0 auto 30px auto;
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    thead tr {
      background-color: #007bff;
      color: white;
      text-align: left;
      font-weight: 600;
    }
    th, td {
      padding: 12px 15px;
      border-bottom: 1px solid #ddd;
    }
    tbody tr:hover {
      background-color: #f1faff;
    }
    td[style] {
      font-weight: 700;
      text-align: center;
    }
    a.back-link {
      display: block;
      max-width: 900px;
      margin: 0 auto;
      text-decoration: none;
      color: #007bff;
      font-weight: 600;
      font-size: 16px;
      transition: color 0.3s ease;
    }
    a.back-link:hover {
      color: #0056b3;
    }
    @media (max-width: 600px) {
      th, td {
        padding: 8px 10px;
      }
    }
  </style>
</head>
<body>

  <h2>Logi prób logowania</h2>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Login</th>
        <th>IP</th>
        <th>Status</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      <% logs.forEach(log => { %>
        <tr>
          <td><%= log.id %></td>
          <td><%= log.username %></td>
          <td><%= log.ip_address %></td>
          <td style="color: <%= log.success ? 'green' : 'red' %>;">
            <%= log.success ? '✔ Sukces' : '✘ Błąd' %>
          </td>
          <td><%= new Date(log.created_at).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <a href="/admin" class="back-link">Powrót do panelu</a>

</body>
</html>
